# Change server port to 80 (default HTTP port)
server.port=80

# Optional: Set context path if needed
# server.servlet.context-path=/finance

# Optional: Logging level
logging.level.org.springframework=INFO

